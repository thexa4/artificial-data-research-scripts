#!/bin/bash

all="false"
while ! $all; do
	all="true"
	for i in $@; do

		
		echo $i
		[ -f /scratch/mmaton/experiment/0.$i/done ] && continue

		all="false"

		./run ./do_experiment $i
		echo "sleep"
		sleep 3

	done
done
